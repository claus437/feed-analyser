CREATE TABLE ARTICLES  (ID INT IDENTITY, SOURCE VARCHAR(255), PUBLISHED DATETIME, TITLE VARCHAR(255), DESCRIPTION VARCHAR(4096), LINK VARCHAR(255))
CREATE TABLE CHANNELS  (ID INT IDENTITY, URL VARCHAR(255), FETCHED DATETIME)

CREATE TABLE COMPANIES (ID INT IDENTITY, NAME VARCHAR(255))
CREATE TABLE SCORINGS  (ID INT IDENTITY, DATE DATETIME, SCORE INT, ARTICLE_ID INT, COMPANY_ID INT)

INSERT INTO CHANNELS (URL) VALUES ('http://www.dr.dk/nyheder/service/feeds/allenyheder')
INSERT INTO CHANNELS (URL) VALUES ('http://www.dr.dk/nyheder/service/feeds/penge')

