# CREATE USER 'feedanalyser'@'localhost' IDENTIFIED BY 'feedanalyser_'
# GRANT ALL PRIVILEGES ON feedanalyser.* TO 'feedanalyser'@'localhost' WITH GRANT OPTION

#CREATE DATABASE IF NOT EXISTS FEEDANALYSER

CREATE TABLE IF NOT EXISTS ARTICLES  (ID INT PRIMARY KEY AUTO_INCREMENT, SOURCE VARCHAR(255), PUBLISHED DATETIME, TITLE VARCHAR(255), DESCRIPTION VARCHAR(4096), LINK VARCHAR(255))
CREATE TABLE IF NOT EXISTS CHANNELS  (ID INT PRIMARY KEY AUTO_INCREMENT, URL VARCHAR(255), FETCHED DATETIME)
CREATE TABLE IF NOT EXISTS COMPANIES (ID INT PRIMARY KEY AUTO_INCREMENT, NAME VARCHAR(255))
CREATE TABLE IF NOT EXISTS SCORINGS  (ID INT PRIMARY KEY AUTO_INCREMENT, DATE DATETIME, SCORE INT, ARTICLE_ID INT, COMPANY_ID INT)
CREATE TABLE IF NOT EXISTS STOCKS  (ID INT PRIMARY KEY AUTO_INCREMENT, COMPANY VARCHAR(255), VALUE VARCHAR(255), DIFF VARCHAR(255), DATE DATE)
CREATE TABLE IF NOT EXISTS SCORE (ID INT PRIMARY KEY AUTO_INCREMENT, COMPANY_ID INT, KEYWORD VARCHAR(255))
DROP TABLE SCORE
CREATE TABLE IF NOT EXISTS SCORE_KEYWORDS (ID INT PRIMARY KEY AUTO_INCREMENT, COMPANY_ID INT, KEYWORD VARCHAR(255))
ALTER TABLE CHANNELS ADD TYPE VARCHAR(255) NOT NULL DEFAULT 'RSS'
